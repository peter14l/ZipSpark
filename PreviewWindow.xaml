<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ZipSpark_New.PreviewWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Archive Preview">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="BaseAlt"/>
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Padding="24,20,24,16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <StackPanel>
                <TextBlock Text="Archive Contents" Style="{StaticResource TitleTextBlockStyle}" FontWeight="SemiBold"/>
                <TextBlock 
                    x:Name="ArchivePathText"
                    Text="" 
                    Style="{StaticResource BodyTextBlockStyle}" 
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Margin="0,4,0,0"
                    TextTrimming="CharacterEllipsis"/>
            </StackPanel>
        </Grid>

        <!-- Content -->
        <Grid Grid.Row="1" Padding="24,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Search and Filter -->
            <Grid Grid.Row="0" Margin="0,0,0,16" ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <AutoSuggestBox 
                    x:Name="SearchBox"
                    Grid.Column="0"
                    PlaceholderText="Search files..."
                    QueryIcon="Find"
                    TextChanged="SearchBox_TextChanged"/>

                <Button 
                    x:Name="SelectAllButton"
                    Grid.Column="1"
                    Content="Select All"
                    Click="SelectAllButton_Click"/>
            </Grid>

            <!-- File List -->
            <TreeView 
                x:Name="FileTreeView"
                Grid.Row="1"
                SelectionMode="Multiple"
                CanDragItems="False"
                CanReorderItems="False"
                ItemInvoked="FileTreeView_ItemInvoked">
                <TreeView.ItemTemplate>
                    <DataTemplate>
                        <TreeViewItem>
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon 
                                    Glyph="&#xE8B7;" 
                                    FontSize="16"
                                    Foreground="{ThemeResource SystemAccentColor}"/>
                                <TextBlock Text="{Binding Name}"/>
                                <TextBlock 
                                    Text="{Binding SizeText}" 
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Margin="8,0,0,0"/>
                            </StackPanel>
                        </TreeViewItem>
                    </DataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>

            <!-- Summary -->
            <Grid Grid.Row="2" Margin="0,16,0,0" Padding="12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Total Files" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock x:Name="TotalFilesText" Text="0" Style="{StaticResource SubtitleTextBlockStyle}" FontWeight="SemiBold"/>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="Selected" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock x:Name="SelectedFilesText" Text="0" Style="{StaticResource SubtitleTextBlockStyle}" FontWeight="SemiBold"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Total Size" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock x:Name="TotalSizeText" Text="0 MB" Style="{StaticResource SubtitleTextBlockStyle}" FontWeight="SemiBold"/>
                </StackPanel>
            </Grid>
        </Grid>

        <!-- Footer -->
        <Grid Grid.Row="2" Padding="24,16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                <Button Content="Cancel" Click="CancelButton_Click"/>
                <Button Content="Extract Selected" Style="{StaticResource AccentButtonStyle}" Click="ExtractButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
